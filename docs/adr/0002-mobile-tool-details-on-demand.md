# 工具详情按用户展开读取

手机默认同步正文、工具摘要和有序状态，完整工具参数及结果通过既有详情引用按用户展开读取。工具参数大小不改变此规则；历史、增量和实时转换必须一致。审批必要内容保留原有自动展示行为，聊天和思考正文正常加载。

仅让工具卡片默认折叠不能减少已经内联下发的数据，服务端和 Android 归一化器必须共同维护此约定。点击后的详情应能自包含地分页阅读，不能只返回另一个不可操作的引用。已经下载的页保留在容量有界的加密缓存中，详情缺失或失败不改变会话的已持久化事件进度。
