# 通过插件复用浏览器订阅连接

Browser 与 Desktop 共用本机页面，多个持久 HTTP 订阅会与目录等普通请求竞争浏览器连接。使用一个页级 WebSocket 承载多个逻辑订阅，普通请求保持现有 HTTP 接口；此变更位于认证插件和官方传输接缝，不修改 dsh-core 或可见界面。

仅把主机清单放进启动脚本不能解决后续目录请求的连接竞争，因此清单预置和订阅连接复用一起验收。逻辑流独立取消和有界排队，物理连接失败交由已有恢复入口处理，写请求不重放。保留旧 HTTP 路由用于资源包兼容，新页面不重新建立每流一条 HTTP 连接的路径。

