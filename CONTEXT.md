# DSH 远程会话

本词汇表描述 Browser、Desktop 与 Android 共用的远程会话概念。

## 术语

**主机**：拥有独立会话数据和运行状态的 DSH 实例。资源身份包含所属主机，界面中选中的主机不改变已有资源的归属。

**权威历史**：服务端记录的有序会话事实。客户端只有在必要内容成功持久化后才确认同步进度。

**正文**：用户消息、助手回答和思考中供阅读的内容。正文不包含工具的完整参数或执行输出。

**工具摘要**：识别一次工具调用及其进展所需的名称、简短操作说明、状态、耗时和错误摘要。

**工具详情**：工具调用的完整参数、命令、结果、原始输出或完整差异。手机用户主动展开后才读取详情。

**审批必要内容**：用户决定是否允许操作所需的命令、路径和操作依据。其展示遵循原有审批流程，不受普通工具详情默认折叠规则限制。

**首屏预览**：从已缓存正文产生的可读近期内容片段。它是显示辅助数据，不是权威历史，也不证明会话已经同步完成。

**缓存准备**：将已有缓存整理为可快速读取的形式，并保留原有完整内容。准备成功与缓存会话首屏性能通过是两个独立验收结果。
